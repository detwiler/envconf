name: cicd
on: [push]
jobs:
  cicd:
    name: CI/CD
    runs-on: ubuntu-latest
    container: ${{ matrix.image }}
    strategy:
      matrix:
        include:
          - image: alpine:latest
            vim-ycm-owner: detwiler
            vim-ycm-ref: llvm-10
          - image: fedora:latest
            vim-ycm-owner: ycm-core
            vim-ycm-ref: master
          - image: ubuntu:rolling
            vim-ycm-owner: ycm-core
            vim-ycm-ref: master
    env:
      USER: ${{ github.repository_owner }}
      VIM_YCM_OWNER: ${{ matrix.vim-ycm-owner }}
      VIM_YCM_REF: ${{ matrix.vim-ycm-ref }}
    steps:
      - name: Upgrade Packages
        run: |
          os_id=`sed -ne 's/^ID=//p' /etc/os-release`
          case $os_id in
              alpine) apk update && apk upgrade && apk add git;;
              fedora) dnf --assumeyes upgrade && dnf --assumeyes install git-core which;;
              ubuntu) apt-get --assume-yes update && apt-get --assume-yes upgrade && apt-get --assume-yes install git;;
          esac

      - name: Checkout
        uses: actions/checkout@v2

      - name: Install Prerequisites
        run: build-aux/install-prereqs

      - name: Bootstrap
        run: ./bootstrap
        env:
          ENVCONF_BOOTSTRAP_INSTALL_REQS: true

      - name: Configure
        run: ./configure --prefix=$RUNNER_TEMP/.local --sysconfdir=$RUNNER_TEMP/.config

      - name: Build
        run: make

      - name: Test
        run: make distcheck

      - name: Install
        run: make install

      - name: Integration Test
        run: |
          vim -es -u $RUNNER_TEMP/.config/vim/vimrc -i NONE -c PlugInstall -c qa
          find $RUNNER_TEMP/.config/vim/plugged/YouCompleteMe -name 'ycm_core.so' | grep .
