" vim: set ft=vim :

set runtimepath^=%vimconfdir%
set runtimepath+=%vimconfdir%/after
if exists('&viminfofile')
  set viminfofile=%vimconfdir%/viminfo
else
  set viminfo+=n%vimconfdir%/viminfo
endif
set directory^=%vimconfdir%/swap//
set backupdir^=%vimconfdir%/backup//
set undodir^=%vimconfdir%/undo//
