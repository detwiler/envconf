#! /bin/bash

# shopt shell options

shopt -s cdable_vars
shopt -s cdspell
shopt -s mailwarn

# Use case-insensitive filename globbing
shopt -s nocaseglob

# Use case-insensitive filename globbing
shopt -s extglob

# Make bash append rather than overwrite the history on disk
shopt -s histappend
