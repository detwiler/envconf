# Makefile.am

include $(top_srcdir)/rules.mk

# data/script variables
gpgconf = gpg.conf

# directory variables
gnupgdir = $(homedir)/.gnupg

# static data/scripts (not generated; require explicit distribution)
static_data = $(gpgconf)

dist_gnupg_DATA = $(static_data)
